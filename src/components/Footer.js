import React from 'react';

let styles = {
  backgroundImage: "url(../images/spottr_banner.png)"
};

const Footer = () => {
  return (
    <footer className={styles}>spottr footer logo</footer>
  )
}

export default Footer;
